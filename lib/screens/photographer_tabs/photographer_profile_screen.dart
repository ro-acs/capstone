import 'package:flutter/material.dart';

class PhotographerProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Edit Photographer Profile"));
  }
}
