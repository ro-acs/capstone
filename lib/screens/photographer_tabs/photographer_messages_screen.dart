import 'package:flutter/material.dart';

class PhotographerMessagesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Messages with Clients"));
  }
}
