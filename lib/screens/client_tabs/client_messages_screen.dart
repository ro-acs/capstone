import 'package:flutter/material.dart';

class ClientMessagesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Client Messages Page"));
  }
}
